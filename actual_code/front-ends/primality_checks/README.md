# Notification
There is a hidden .tester.c file in this folder.

When you
```bash
make tester
```

You get tester.

Feed tester as an argument a binary prime table and it will check every number up to 10000 for primality using table\_lookup\_library's prime()
